---
title: "Quickstart"
description: "Your first Dequan trade, end to end, in under a minute."
icon: "rocket"
---

You don't need an account. You don't need a credit card. You don't need to read a manual. You need a Solana wallet and one minute.

## 1. Open Dequan

<Card title="snipe.dequan.xyz" icon="globe" href="https://snipe.dequan.xyz" horizontal>
  Hosted on Cloudflare's global edge. Loads in any browser, on any device.
</Card>

## 2. Connect a wallet and fund your Lightning Wallet

Phantom, Solflare, or Backpack. One tap, one signature. We never see your private key. There is no "create a Dequan account" step — the wallet **is** your account.

<Note>
The signature you sign during connect is a **proof-of-ownership challenge**, not a transaction. It moves zero SOL and authorises nothing on-chain. It exists so we can prove you control the wallet without holding any keys.
</Note>

Once connected, **follow the on-screen prompts to fund your Lightning Wallet.** This is the single best thing you can do to get the fastest possible experience from your very first trade. Lightning Wallet eliminates the wallet-signing popup on every buy and sell — instead of adding several seconds per trade waiting for you to approve a popup, trades fire instantly with a single tap.

You choose how much to fund. It is withdrawable any time. The app will guide you through it step by step.

<Tip>
Not ready to fund yet? You can skip this step and trade using your main wallet's manual signing prompts. Every feature is still available — trades just require a popup approval each time.
</Tip>

## 3. Enter the Pump Zone

This is the home view. Tokens flow across the field as live data arrives. Position on the field tells you everything you need to know:

- **Horizontal** — market cap. Smaller tokens on the left, larger on the right.
- **Vertical** — velocity. Tokens rising fast in price drift upward; tokens cooling drift downward.
- **Glow & motion** — fresh trades flash. Sustained climbers leave a trail.

Your eye will pick out movement before any leaderboard could rank it. That is the entire design.

<Tip>
You can switch into **Sniper List** mode for a traditional analyst view at any time. The same data, presented as a sortable table with charts inline.
</Tip>

## 4. Tap a token

Hovering over a token locks the token in place and shows a quick stats card. Tapping a token opens the candle drawer with a live candle chart, holders breakdown, dev history, recent trades, and a buy panel with **Quick Buy** preset to your default size.

The instant the drawer opens, Dequan immediately runs honeypot checks by reading the token contract and begins preparing the trade in the background — fetching a quote, warming the route, lining up everything execution needs. Other platforms run these ops at buy time. Dequan anticipates your next move and preprocesses as much as possible before you've ever decided to buy.

## 5. Buy

Press **Buy**. Done.

Behind the scenes your transaction is dispatched across multiple landing paths simultaneously. Whichever lane lands first becomes your fill. The token immediately appears as a **'B'** on the candle chart.

## 6. Exit when you're ready

- **One-tap presets** — sell 25%, 50%, 75%, or all of a position from the lane itself.
- **Sell** — single button, plain English, no menus.
- **Outcome card** — every closed trade ends with an onscreen summary of the trade.

That is the whole loop. You are now trading on Dequan.

## What just happened, technically

Behind that minute, Dequan's systems quietly:

- Streamed live token metrics from a fan-out pipeline so your browser never opens upstream connections directly.
- Pre-warmed your trade so the click-to-signature path was as short as the network would allow.
- Race-broadcast your signed transaction across multiple landing paths to maximize the odds of landing in the next block.
- Validated the token was not a honeypot before you even saw the chart.
- Watched the pool's liquidity in real time and would have warned you instantly if it drained.
- Logged every checkpoint of the trade so you can see exactly where time was spent.

You didn't see any of that. You shouldn't have to. **Dequan does the engineering so you can do the trading.**

## Does my browser add latency?

When buying tokens on the curve, speed is King. Ever wondered if using Brave is better than using Chrome or Edge, or simply how much time is added on to the trade from the browser? Dequan has a tool for that! [Try it out](https://snipe.dequan.xyz/latency-map) and see what kind of speed others have around the world.
